/**
 * Hero module — intentionally static.
 * No particles, parallax, corner animations, or intro motion.
 * Matches the production handoff requirement.
 */
const Hero = (() => {
  function init() {
    // The hero is intentionally static.
  }

  return { init };
})();
